import { ItemTitleDirective } from './item-title.directive';

describe('ItemTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new ItemTitleDirective();
    expect(directive).toBeTruthy();
  });
});
